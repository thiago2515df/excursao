# Porta do servidor
PORT=3000

# URL pública da aplicação (importante para links de imagens)
# Em desenvolvimento local: http://localhost:3000
# Em produção Railway: https://seu-app.up.railway.app
PUBLIC_URL=http://localhost:3000

# Chave secreta para JWT (GERE UMA NOVA!)
# Execute: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=change-this-to-a-random-secret-key

# Ambiente de execução
NODE_ENV=development
